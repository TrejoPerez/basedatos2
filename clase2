/*
CREATE TABLE usuario1(
ID_USUARIO1 NUMBER(38) NOT NULL,
NOMBRE VARCHAR2(120),
EMAIL VARCHAR(90),
SUELDO_BASE FLOAT(126),
CONSTRAINT ID_USUARIO1 PRIMARY KEY(ID_USUARIO1)
);
*/
--CREAR LA TABLA DE DIRECCION
/*
CREATE TABLE DIRECCION(
ID_DIRECCION INTEGER,
ID_USUARIO1 INTEGER,
CALLE VARCHAR2(180),
NUMERO INTEGER,
COLONIA VARCHAR2(250),
MUNICIPIO VARCHAR2(180),
CP INTEGER,
CONSTRAINT ID_DIRECCION_PK PRIMARY KEY(ID_DIRECCION),
--REFERENCES tabla(primary key de la tabla)
--lAS CONSTRAINT DEBEN DE TENER NOMBRES DIFERENTES A LAS LLAVAES PRIMARIAS Y FORANEAS
CONSTRAINT ID_USUARIO1_FK FOREIGN KEY (ID_USUARIO1) REFERENCES usuario1(ID_USUARIO1)
);
*/

create sequence usuario1
start with 1
increment by 1
nomaxvalue;

create sequence direccion
start with 1
increment by 1
nomaxvalue;

--RECREAR EL PROCEDIMIENTO

create or replace PROCEDURE GUARDAR_USUARIO1(
MY_ID_USUARIO1 OUT INTEGER, MY_NOMBRE IN VARCHAR2,
MY_EMAIL IN VARCHAR2, MY_SUELDO_BASE IN FLOAT);
----PROCEDURE FOR GUARDAR_DIRECCION
create or replace PROCEDURE GUARDAR_DIRECCION(
);
